import { Calendar1Icon } from "lucide-react";

const NavBar = () => {
  return (
    <nav className="bg-blue-400 p-3 flex items-center gap-2 text-white font-semibold">
      <Calendar1Icon size={32} />
    </nav>
  );
};

export default NavBar;
